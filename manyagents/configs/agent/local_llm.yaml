# @package _global_
# Backward compatibility alias — use agent=hf instead

defaults:
  - hf@_here_
  - _self_

# Override name/adapter for backward compat with experiment logs
agent:
  name: local_llm
  adapter: hf
