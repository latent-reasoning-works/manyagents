# @package _global_
# Backward compatibility alias — use agent=hf with model override instead

defaults:
  - hf

agent:
  name: local_llm_70b
  adapter: hf
  config:
    model: llama-3.3-70b
    temperature: 0.1
    max_new_tokens: 2000
